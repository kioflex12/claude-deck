# Roadmap — Claude Deck

Статус на 2026-08-03. Историческая заметка: изначально планировался форк `nimbalyst/nimbalyst`,
но пошли своей тонкой оболочкой (Electron-main + `server.mjs` + Agent SDK) — свой UI без чужих
trust/consent-гейтов. Поэтому пункты Фаз 0-3 доставлены **своей реализацией**, а не форком.

Инвариант: каждый видимый элемент — **гиперссылка** в исходную систему (ветка→GitLab, MR→страница MR,
сборка→TeamCity, задача→Jira).

## Фаза 0 — Bootstrap ✅
- [x] Прототип UI/UX → перерос в рабочее приложение (`index.html` + `web/js/*`).
- [x] Репозиторий `github.com/kioflex12/claude-deck`, сборка на Windows (`npm i` / `electron-builder`).
- [~] Форк nimbalyst — отменён в пользу своей оболочки (см. заметку выше).

## Фаза 1 — Данные (read-only) ✅
- [x] Доска сессий (колонки по dev-workflow-стадии + статусу Jira).
- [x] Привязка задача↔сессия↔ветки↔MR из `dev-workflow/WO-XXXX.json` (+ структурные поля `.jsonl`).
- [x] Статус задачи из Jira (REST, `/api/jira`).
- [x] Статусы сборок Android/iOS из TeamCity + deep-link в билд (`/api/build`).
- [x] Статусы MR из GitLab + ссылка на MR (`/api/mrs`).
- [x] Обвязка всего ссылками.

## Фаза 2 — Сессии ✅
- [x] Карточка↔сессия; «Продолжить»/чат через Agent SDK (SSE-стрим, не `claude --resume` shell).
- [x] Полноэкранная сессия: чат слева + контекст-рейл справа.
- [x] Индикатор окна контекста (токены) + «Форкнуть сессию».

## Фаза 3 — Скиллы и MCP ✅
- [x] Вкладка «Скиллы»: реальные `.claude/skills/*/SKILL.md` по cwd, категории, поиск, вставка `/команд`.
- [x] Вкладка «MCP»: список серверов из реального окружения, статус, детали.

## Фаза 4 — Ускорители
- [x] Командная палитра Ctrl K (задачи / скиллы / MCP / виды).
- [x] Монитор сборок TeamCity (идёт/очередь/упала) — в рейле сессии (`loadBuilds`, live-поллинг 15с).
- [ ] Лента «Требует внимания» (красные сборки, незакоммичено, device-verify, блокеры).
- [ ] Статус окружений squad/preprod/preupdate (k8s/api).
- [ ] Быстрые действия на карточке (отчёт в Jira, создать MR, форк, деплой).

## Что осталось (актуально)
- **TECH-4 (надёжность)** — в работе: ретраи/бэкофф + видимость падений интеграций (см. BACKLOG.md).
- **Фаза-4 фичи** (три пункта выше) — net-new, по желанию.
- **macOS подпись/нотаризация** — опционально.
